{
  "name": "milkman-root",
  "version": "1.0.0",
  "description": "Milkman Root Management",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../adminpanel && npm install && cd ../backend && python -m pip install -r requirements.txt",
    "build:all": "cd frontend && npm run build && cd ../adminpanel && npm run build",
    "start": "pm2 start ecosystem.config.js",
    "start:prod": "npm run build:all && pm2 start ecosystem.config.js --env production",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop ecosystem.config.js",
    "pm2:restart": "pm2 restart ecosystem.config.js",
    "pm2:delete": "pm2 delete ecosystem.config.js",
    "pm2:logs": "pm2 logs",
    "pm2:monit": "pm2 monit",
    "pm2:save": "pm2 save",
    "pm2:startup": "pm2 startup"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  }
}
